## Исследование архитектурного решения (SpreadScope)

## 📌 О проекте

**SpreadScope** — веб-приложение для мониторинга спредов между биржами и конвертации активов (фиат + криптовалюты).  
Система позволяет получать котировки с нескольких бирж, рассчитывать спреды по одинаковым торговым парам, отображать результаты в web-интерфейсе, а также выполнять конвертацию активов.

## Материалы
- [Схема To Be (Mermaid)](./to-be-mermaid.md)
- [Схема As Is (Mermaid)](./as-is-mermaid.md)

---

# Часть 1. Проектирование архитектуры (To Be)

## 1. Тип приложения

Разрабатываемая система относится к типу:

- **Web-приложение (клиент-серверное)**
- С элементами **real-time взаимодействия** (WebSocket-стриминг котировок)
- Архитектурный стиль: **многослойная архитектура**
  - слой представления (Frontend UI)
  - слой API / контроллеров
  - слой бизнес-логики (services)
  - слой источников данных
  - слой хранения данных

---

## 2. Стратегия развёртывания

### Текущий вариант (MVP / Sprint 1)
- Один backend-сервис на **FastAPI**
- Frontend (HTML/CSS/JS) отдается этим же backend’ом
- Локальный запуск через `uvicorn`
- Хранение временных данных в памяти процесса (`in-memory cache`)
- Локальные данные конвертера — JSON-файл

### Планируемый вариант (последующие спринты)
- Frontend и Backend разворачиваются отдельно (распределенный тип)
- Backend подключается к БД (SQLite → PostgreSQL)
- Выделяется отдельный polling/worker для получения котировок с бирж
- Возможна контейнеризация (Docker) и деплой на VPS/облачную платформу

---

## 3. Обоснование выбора технологий

### Frontend
- **HTML5 / CSS3 / JavaScript (ES6+)**
- Причины выбора:
  - быстрое создание MVP
  - низкий порог сложности
  - прозрачная логика без дополнительных фреймворков
  - удобно для демонстрации в учебном проекте

### Backend
- **Python + FastAPI**
- Причины выбора:
  - высокая скорость разработки API
  - встроенная поддержка async/await
  - удобная работа с WebSocket
  - автоматическая документация (`/docs`)

### Получение данных
- **aiohttp**
- Причины выбора:
  - асинхронные HTTP-запросы
  - подходит для регулярного опроса нескольких API бирж

### Хранение данных
- **JSON** (ранний этап, MVP)
  - локальные котировки/курсы для конвертера и fallback
- **SQLite / PostgreSQL** (план)
  - хранение пользователей, избранного, истории действий, журнала сделок

---

## 4. Показатели качества (Quality Attributes)

Для системы SpreadScope ключевыми являются следующие характеристики качества:

### 4.1 Производительность
- Асинхронный polling котировок с бирж
- In-memory cache (`PRICES`, `META`) для быстрых ответов API
- Возможность push-обновлений через WebSocket

### 4.2 Надёжность
- Обработка ошибок внешних API (биржи могут быть временно недоступны)
- Фоновый polling не завершает работу при единичных ошибках
- Локальные курсы конвертера в JSON-файле как fallback

### 4.3 Расширяемость
- Новые биржи можно добавлять через конфигурацию `EXCH_DEF`
- Логика расчёта спредов вынесена в отдельный модуль `services/spreads.py`
- Слои UI / API / services разделены по директориям

### 4.4 Сопровождаемость
- Модульная структура проекта (`backend`, `frontend`, `services`, `data`)
- Разделение бизнес-логики и HTTP-слоя
- Понятные endpoint’ы (`/api/spreads`, `/api/quotes`, `/health`)

### 4.5 Удобство использования
- Единая web-страница MVP с таблицей спредов и конвертером
- Фильтр по символу
- Простая схема взаимодействия пользователя с системой

---

## 5. Пути реализации сквозной функциональности (Cross-Cutting Concerns)

### 5.1 Конфигурация
- Настройки через переменные окружения:
  - список бирж
  - интервалы polling
  - таймаут HTTP-запросов

### 5.2 Обработка ошибок
- `try/except` в polling-циклах
- Возврат корректных ответов API даже при частичной недоступности данных

### 5.3 Логирование (планируемое расширение)
- Логирование ошибок получения котировок
- Логирование состояния polling/worker
- Логирование запросов и времени ответа

### 5.4 Валидация данных
- Валидация пользовательского ввода в конвертере (число > 0)
- Фильтрация некорректных котировок при нормализации
- Пропуск записей без достаточных данных для расчёта спреда

### 5.5 Мониторинг / Health-check
- Endpoint `/health` для проверки состояния сервиса и объёма кэша котировок

### 5.6 Безопасность (план)
- Настройка CORS
- Введение авторизации для персональных функций (watchlist, history)
- Разделение публичных и приватных endpoint’ов

---

## 6. Структурная схема приложения (To Be)

Архитектура To Be представлена на схеме **To Be**:  
- [Открыть Mermaid-схему To Be](./to-be-mermaid.md)

Ключевые функциональные блоки:
- Browser / User
- Frontend UI
- Backend API (FastAPI)
- Application Services
- Data Source Layer (биржи, локальные данные)
- Storage Layer (in-memory + DB)
- Authentication / User Data (планируемые модули)

---

# Часть 2. Анализ архитектуры (As Is)

## Текущая реализация (по итогам Sprint 1 / MVP)

Архитектура As Is представлена на схеме **As Is**:  
- [Открыть Mermaid-схему As Is](./as-is-mermaid.md)

На текущем этапе в проекте реализованы следующие компоненты:

### Backend
- `backend/app.py`
  - FastAPI-приложение
  - REST API (`/health`, `/api/quotes`, `/api/spreads`, `/api/converter/rates`)
  - WebSocket endpoint (`/ws`)
  - фоновый polling котировок
  - in-memory cache котировок и мета-информации
- `backend/exchanges.py`
  - конфигурация источников котировок (`EXCH_DEF`)
  - парсеры ответов API разных бирж
  - нормализация записей для frontend
- `backend/services/spreads.py`
  - расчёт спредов между биржами

### Data
- `backend/data/rates.json`
  - локальные курсы для конвертера (MVP / fallback)

### Frontend
- `frontend/index.html`
- `frontend/style.css`
- `frontend/app.js`

Функциональность frontend:
- таблица спредов
- фильтр по symbol
- конвертер (локальные курсы)
- отображение курса пары и результата конвертации

---

## Компоненты и зависимости (As Is)

Текущая архитектура имеет модульный, но упрощённый вид:
- `app.py` является центральной точкой координации
- `exchanges.py` предоставляет конфигурацию и парсинг внешних API
- `spreads.py` реализует отдельную бизнес-операцию (расчёт спредов)
- frontend напрямую обращается к backend endpoint’ам

Архитектура As Is представлена на схеме **as-is** (см. файл `as-is-mermaid.md` / изображение `as-is.png`).

---

# Часть 3. Сравнение архитектур и рефакторинг

## 1. Сравнение As Is и To Be

### Совпадения
- Разделение на frontend и backend реализовано
- Выделен модуль источников данных (биржи)
- Выделен модуль расчёта спредов
- Реализован публичный API и WebSocket
- Реализован базовый конвертер

### Отличия
- Отсутствует авторизация и персональные данные пользователей
- Нет постоянного хранилища (SQLite/PostgreSQL)
- Нет выделенного слоя репозиториев
- Polling и API находятся в одном процессе/модуле
- Нормализация пар реализована частично (без полноценной единой модели `AssetPair`)
- Отсутствует развитое логирование и тестирование

---

## 2. Причины отличий

Основные причины расхождения архитектуры As Is и To Be:

1. **Ограничение по времени первого спринта**
   - задача Sprint 1 — получить рабочий MVP, а не финальную архитектуру

2. **MVP-подход**
   - сначала реализована ключевая ценность проекта:
     - получение котировок
     - расчёт спредов
     - отображение в UI
     - конвертер
   - вторичные функции (авторизация, история, избранное) отложены

3. **Переиспользование существующего кода**
   - модуль получения котировок с бирж был адаптирован под новый проект, что ускорило разработку, но сделало архитектуру менее “чистой” на текущем этапе

4. **Упрощение инфраструктуры**
   - использование `in-memory` и JSON вместо БД позволило быстрее подготовить демонстрационный вариант

---

## 3. Пути улучшения архитектуры (рефакторинг)

### 3.1 Выделение адаптеров бирж
Создать интерфейс/базовый класс адаптера и отдельные реализации:
- `GateAdapter`
- `MexcAdapter`
- `OurbitAdapter`

Это снизит связность `app.py` с деталями внешних API.

### 3.2 Разделение polling и API
Вынести получение котировок в отдельный сервис (worker), а backend API оставить только для чтения и выдачи данных.

### 3.3 Введение слоя репозиториев
Добавить слой доступа к данным:
- `QuotesRepository`
- `WatchlistRepository`
- `UserHistoryRepository`
- `TradeJournalRepository`

### 3.4 Введение постоянного хранилища
- SQLite на этапе MVP персональных функций
- PostgreSQL при расширении проекта

### 3.5 Улучшение нормализации пар
Ввести единую модель торговой пары (`AssetPair`) и преобразование форматов:
- `BTC_USDT`
- `BTCUSDT`
- `BTC-USDT`

### 3.6 Тестируемость
Добавить unit-тесты:
- расчёт спредов (`calculate_spreads`)
- нормализация котировок
- обработка частично некорректных данных

### 3.7 Наблюдаемость и диагностика
- структурное логирование
- метрики по запросам к биржам (успех/ошибка)
- контроль задержки обновления данных

---

# Вывод

В рамках исследования архитектурного решения для проекта **SpreadScope** была сформирована целевая архитектура (**To Be**) и проанализирована текущая реализация (**As Is**) на основе кода Sprint 1.

Результаты показали, что текущая версия системы уже содержит основные архитектурные элементы:
- frontend/backend разделение,
- модуль источников данных,
- модуль расчёта спредов,
- API и WebSocket,
- базовый конвертер.

При этом ряд важных частей целевой архитектуры (авторизация, постоянное хранилище, слой репозиториев, отдельный worker) пока не реализованы, что объясняется ограничениями первого спринта и выбранным MVP-подходом.

Выделены конкретные направления рефакторинга и развития архитектуры, которые могут быть реализованы в следующих спринтах без полной переработки проекта.
